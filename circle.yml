version: 2
jobs:
  build:
    docker:
     - image: circleci/android:api-25-alpha
    working_directory: ~/demo_android
    steps:
      - checkout
      
      - run:
          name: Download Dependencies
          command: ./gradlew androidDependencies

